# Only needed when the API is on a different origin than the UI (local dev).
# In production Caddy serves both on one hostname and the default "/api"
# relative path is correct, so leave this unset.
VITE_API_BASE_URL=http://localhost:5000
